{
  "name": "bcns-outreach-pipeline",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "test": "tsx --test tests/smoke.test.mjs",
    "job": "tsx jobs/run.mjs",
    "lint": "for f in jobs/*.mjs lib/*.mjs tests/*.mjs; do [ -f \"$f\" ] || continue; node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "imapflow": "^1.0.198",
    "mailparser": "^3.7.4",
    "nodemailer": "^9.0.5",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1"
  }
}
