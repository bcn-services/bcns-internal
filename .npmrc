# No private registry is needed: the @nseluga/* packages were dropped when this
# repo became a jobs runner. Left empty deliberately — re-add the registry line
# only if a private dependency comes back.
