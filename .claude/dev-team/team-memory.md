# Dev-team memory log

## 2026-08-24 19:27 — dev-team-auto — Migration 0009 — the automation schema
- **Outcome:** DONE — 2 attempts — caution: yes — team: dt-engineer opus/medium (build) + dt-qa opus/medium (verify) + dt-review opus/medium (review) + dt-engineer opus/medium (fix pass) + dt-qa opus/medium (re-verify) + dt-review opus/medium (re-review) — branch `item/0009-automation-schema`, commit `4493283`
- **What happened:** Built `supabase/migrations/0009_automation_schema.sql` + down: 4 additive columns, 3 new tables (`inbox_items`, `lead_targets`, `job_runs`), 7 policies. Attempt 1 passed QA but review found one Important; attempt 2 fixed it and re-verification came back clean. Suite 219 -> 277 pass, 0 fail, 0 skipped, 34 suites.
- **What worked:** QA parsing the migration SOURCE rather than the catalog for the qualification guardrail — `pg_get_expr` deparse is search_path-sensitive and gives a false green. Mutation-checking every guardrail test (unqualify a call, confirm the test fails) caught that a regex loosening was still sound. The reviewer's second pass was scoped to `git diff <prior-commit>..HEAD` and finished in a third the tokens of the first.
- **What failed:** Review attempt 1 found an Important the builder and QA both missed: widening the `account_activity.kind` CHECK silently met 0002's pre-existing permissive `account_activity_staff_insert` (`with check (public.is_staff())`), letting any staff member forge `agent_run`/`ai_email_sent` audit rows. Neither the builder nor QA thought to re-check EXISTING policies against a widened constraint. One dt-qa spawn was killed mid-run for running too long; a `SendMessage` resume of a completed agent reported success but produced no live child — respawning fresh was the fix.
- **Remember next run:** Widening a CHECK constraint in this repo means re-auditing every existing RLS policy that writes that column — 0002's policies are permissive and were written against the narrower vocabulary. `create policy` has no `set search_path` clause (it is function-level only); the convention's equivalent is fully qualifying every call (`public.is_admin()`, `public.is_staff()`, `auth.uid()`). Lint reports 5 pre-existing errors, not the 2 the lane brief claims — that figure is stale. A later migration re-creating an earlier migration's policy means reading 0002 alone now misleads; last file wins. Left as accepted Minors: `account_activity_admin_all` (FOR ALL) still lets an admin insert agent kinds (needs an append-only trigger, separate item); the kind CHECK stays `not valid` so the planner cannot use it; `tests/helpers/pg-cluster.mjs` grants `authenticated` but not `anon`, so the anon deny path is argued not test-enforced. An agent left a stray untracked `lib/agent/verbs/` in the worktree — out of this item's scope, never committed.

## 2026-08-31 — dev-team-auto — Item 8: Wire sourcing to the real world (Places client + budget reader)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/medium — branch `item/0008-places`, commit `ef96c77` (green checkpoint), `b946b85` (lane bookkeeping)
- **What happened:** Added `lib/places.mjs` (Places API New text search, bearer token from `GOOGLE_OAUTH_ACCESS_TOKEN`, no API key) and `lib/budget.mjs` (`readBudget()` → `{remaining, month}`, throws on a malformed billing response rather than returning a number). Extracted an exported `buildDeps(env)` in `jobs/run.mjs` so the deps object is testable without running a job. Suite 75 → 86 pass / 0 fail.
- **What worked:** Mutation-checking criterion 1 rather than trusting a green assertion. Extracting `buildDeps(env)` as an export made the "deps carry places and readBudget when the token is present" criterion directly assertable instead of inferred.
- **What failed:** Nothing in the loop. But the item surfaced a latent defect the plan did not anticipate: `jobs/source.mjs` (item 5) consumed `r.placeId` / `r.domain` while the LANE-spec'd Places row shape is `place_id` / `website`. Item 5's own tests passed because its fake client used item 5's shape. The first live Monday run would have inserted zero rows, silently.
- **Remember next run:** The preflight's declared-test-command line implied a 76-test baseline; the real floor was 75. Take the floor from the suite's own `# pass` line, not the file count. **Defect family carried forward: an item's fake fixture encodes the shape the item's own author assumed, so a producer/consumer field-name mismatch between two items passes both items' tests and fails only live.** Every remaining wiring item (9, 10, 11, 12, 13) hands a row shape to the next — assert the field names against LANE.md's stated shape, not against the neighbouring module's fake.

## 2026-08-31 — dev-team-auto — run correction: stale working branch
- **Outcome:** N/A — process defect caught mid-run, before item 9
- **What happened:** Start Up read LANE.md on `main`, then switched the working branch to `outreach-pipeline` to honour the lane's "never commit to main" rule. `outreach-pipeline` was 6 commits BEHIND `main` (no unique commits of its own), so item 8's worktree carried a stale LANE.md. Item 8's own spec block was byte-identical on both branches and the engineer was given `main`'s text in its prompt, so the build is sound; what was lost was surrounding context (the `~/os`-clone convention, the Claude CLI install step, the hardened specs for items 9-13, and the added item 14). Fixed by fast-forwarding `outreach-pipeline` to `main` and rebasing `item/0008-places` onto it.
- **What worked:** Noticing that the caution flags visible in the worktree disagreed with the ones read at Start Up. A disagreement between two reads of the same file is a branch-divergence signal, not a memory error.
- **What failed:** Trusting the README's and LANE_PROGRESS's claim that `outreach-pipeline` was "level with `main`". Both were written before the last 6 commits landed and neither is regenerated.
- **Remember next run:** In this repo `main` is the live contract — Nate merges lane branches to `main` by hand so `workflow_dispatch` works, which means `main` is routinely AHEAD of the lane branch, the reverse of the usual arrangement. Before choosing a working branch, run `git log --oneline <branch>..main` and `main..<branch>` and pick the tip; never take a prose claim of "level with main" from a README or progress file as fact. `outreach-pipeline` was fast-forwarded to `main` on 2026-08-31 and is level again as of this run.

## 2026-08-31 15:40 — dev-team-auto — item 9: wire qualification into the clock
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0008-places, 3a587b7
- **What happened:** `SCHEDULES['0 13 * * 1']` became `['source','qualify']` with a new `jobNames()` (`jobName()` still returns the first, so the old clock tests kept passing); `main()` loops a tick's jobs in order on one lazily-built deps object. `buildDeps` gained `fetchPage` (10s deadline, 512KB cap), `claude` gated on `CLAUDE_CODE_OAUTH_TOKEN`, and `verify` gated on `MAIL_FROM`. `qualify` skips on a missing `fetchPage`/`claude` and demotes an undeliverable address (`invalid`/`no-mx`/`rejected`) to `call_due` with `email: null`, phone untouched.
- **What worked:** Extending the schedule value to a list instead of changing `jobName`'s contract kept every existing clock assertion green. Field names were checked against the producing modules (`db.updateBusiness` patch keys, `lib/verify.mjs`'s `{ok,status,address}`), not against neighbouring fakes. 87 → 96 tests, 0 fail, no new test file needed.
- **What failed:** none — single attempt, no rework.
- **Remember next run:** `deps.verify` opens port 25, which GitHub-hosted runners commonly block, so in production it will likely always return `unknown` and verification effectively never fires until the probe moves to a relay — worth a follow-up item. `lib/claude.mjs` is CLI-only via `execFile`; a test asserting "no API key" can just regex the source. `tests/qualify.test.mjs` has a `harness({rows,pages,answer})` helper — reuse it rather than hand-rolling deps.

## 2026-08-31 15:45 — dev-team-auto — item 10 build personalization (jobs/personalize.mjs)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0008-places, 3cb96733a704df3b36b74dead06d23597b98e264
- **What happened:** New `lib/template.mjs` (byte-faithful copy of the fenced template in `~/os/skills/outreach/SKILL.md`, plus `render()`/`isRoleAddress()`), new `jobs/personalize.mjs` (one `claude.ask` per business, thin-facts skip before the call, post-render ban check on `http`/`$`/`demo is ready`, buffer capped at 25 via a new `db.draftedCount`), 16 new tests. Suite 96 → 112 pass, 0 fail.
- **What worked:** Verifying the template copy by extracting the fenced block from SKILL.md and comparing strings in a `tsx -e` one-liner rather than eyeballing — confirmed identical. Rendering one real draft end to end caught the real risk early: `lib/signature.txt` happens to carry `bcn-services.com` with no scheme, so the `http` ban does not nuke every draft; a signature with `https://` would have.
- **What failed:** none.
- **Remember next run:** `businesses` has NO `owner_name` column (0018) — the greeting name comes from `research.owner_name` and is omitted when absent or when the address is a role address. `research` is `{facts, fit, reason}` written by `qualify.mjs` as a JSON string; `personalize` merges `draft` in and must not clobber those keys. The ban list is `BANNED` in `jobs/personalize.mjs` and is checked against the WHOLE rendered draft, so anything later added to `lib/signature.txt` (a `https://` URL, a price) silently rejects every draft — item 11/13 should assert the signature against `BANNED`. `deps.readVoiceRules` is NOT wired into `buildDeps` yet.

## 2026-08-31 16:05 — dev-team-auto — item 11: touch.mjs sender
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0008-places, b757768
- **What happened:** Built `jobs/touch.mjs` (allow-list gate, claim-then-send mailbox round-robin, warming ramp, in-thread bumps, call_due at touches>=3, hand-rolled multipart/alternative), plus `dueTouches`/`activeMailboxes`/`claimMailboxSlot`/`firstOutbound` in `lib/db.mjs`, `SIGNATURE_HTML`/`toHtml` in `lib/template.mjs`, and an SMTP transport factory + `allowedRecipients` in `buildDeps`. 112 -> 131 tests.
- **What worked:** Claim-then-send (conditional `update … where sent_today < cap returning *`) satisfies the concurrency guardrail with no lock. Putting `assertAllowed` as the first statement of the single `deliver()` path made both the removal and the ordering mutation fail loudly. Quoting the real 0018 column names and the real `research.draft` shape into the engineer's prompt kept the fakes honest — no producer/consumer drift with personalize.mjs.
- **What failed:** One mutation SURVIVED on the first check: the call_due test asserted `touches: MAX_TOUCHES` (the imported constant), so raising `MAX_TOUCHES` to 4 moved the assertion with the mutation and stayed green. Fixed by pinning the literal `3` plus an explicit `assert.equal(MAX_TOUCHES, 3)`.
- **Remember next run:** `sent_today` is never reset — a daily reset job or a date column on `mailboxes` is still owed, and until it exists the cap is a lifetime cap, not a daily one. A claimed slot is not released when the send throws (under-sends, never over-sends). `dueTouches` scans `next_touch_at` with no index in 0018. Mutation-check every safety constant against a LITERAL, never against the constant the code exports.

## 2026-08-31 16:30 — dev-team-auto — item 12: poller + reply parser (jobs/poll.mjs)
- **Outcome:** DONE — caution: true, the run's only one — team: dt-engineer + dt-qa + dt-review — commits `cd3d6bb` (QA PASS) → `eba5991` (review fixes) → `bed05dc` (reversal) → `cf32895`/`2da8f3e`/`3a9a13e` (late passes)
- **What happened:** dt-qa PASS at attempt 1; dt-review then found 1 Critical + 5 Important + 4 Minor. The Critical was real and invisible to unit tests: an html-only reply (Outlook, or a Gmail reply carrying an inline image) arrives with `parsed.text === undefined`, and an empty body reads as "not an opt-out" — the exact false negative the item exists to prevent.
- **What worked:** Re-running dt-qa as a DELTA pass scoped to `git diff cd3d6bb..bed05dc` rather than a full re-verify — its green no longer covered HEAD after two fix commits. It re-confirmed all four criteria through the real `run(deps)` entry point and mutation-checked every fix; no safety path stayed green. Fixing the curly-apostrophe gap at ONE choke point in `isOptOut` (fold U+2019/U+02BC after `stripQuoted`) rather than adding a variant to each of 21 patterns — `OPT_OUT_PATTERNS` stayed byte-identical and every future pattern gets it free.
- **What failed:** THE ORCHESTRATOR OVERLAPPED THE NEXT ITEM. It returned a non-standard outcome string ("Waiting on the reversal", later "Waiting on the normalisation fix") instead of DONE/BLOCKED, and kept spawning children AFTER returning — a dt-engineer, then a third dt-qa — while item 13 was already building in the same worktree. Item 12's in-flight work landed inside item 13's commit `70299a7`. The orchestrator also spawned item 14's engineer with `isolation: "worktree"`, putting it in a fresh worktree forked from `outreach-pipeline` that lacked items 8-13 entirely; it was redirected via `SendMessage` + `EnterWorktree`, leaving a stray locked worktree behind.
- **Remember next run:** A `dt-orchestrator` returning anything other than the standard DONE/BLOCKED line has NOT finished — check `ListAgents` for live children before dispatching the next item, and stop orphans. Never `git checkout --` in a shared worktree to restore a mutation check; another agent's uncommitted work may be in the tree (this nearly destroyed live work here and was safe only by luck — the orchestrator had just committed). Restore from a `cp` backup and verify byte-identical. Adjudicated reversal worth keeping: mark an IMAP message seen AFTER the handler returns, not before — a retried message costs a duplicate forward, which is visible, while the other ordering silently drops an opt-out, which never is.

## 2026-08-31 16:40 — dev-team-auto — item 13: notification emails (jobs/notify.mjs)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0008-places, commit `70299a7` (green checkpoint), `d0e3a60` (lane bookkeeping)
- **What happened:** New `jobs/notify.mjs` — four templates (`approvalEmail`, `callTaskEmail`, `meetingEmail`, `quoteEmail`) returning `{subject,text}`, a `notifyKey(row)` dedupe key, and a run loop over `NOTIFY_STAGES = ['drafted','call_due','replied','quoting']`. Sends through poll's existing `createNotifier` (no second sender). `lib/db.mjs` gained `businessesByStage` (via `selectable_businesses`) and `notifiedKeys` (dedupe read off `events`). `SCHEDULES['*/20 8-20 * * 1-5']` is now `['poll','notify']`. Suite 222 → 237 pass / 0 fail (237 includes ~4 tests the concurrent item-12 review pass added to `tests/poll.test.mjs`).
- **What worked:** Reusing `createNotifier`/`assertAllowed` instead of writing a second send path meant the allow-list mutation broke 10 tests across three files, not one. Making the fake `db.updateBusiness` THROW is what turns "notify never changes a stage" from prose into an enforced guardrail. Folding `next_touch_at` into the `call_due` dedupe key is what stops the once-per-stage marker from permanently silencing a re-scheduled call — the item-11 "counter that only increments" family, caught before it shipped.
- **What failed:** Nothing in the loop. Both mutation checks went RED as required (allow-list forced open → 10 fail; dedupe short-circuited → 1 fail), files restored byte-identical from a `cp` backup.
- **Remember next run:** **Two dev-team items ran in the SAME worktree at the same time.** Item 12's orchestrator was still doing a third review pass in `item/0008-places` while item 13 built there — `jobs/poll.mjs` and `tests/poll.test.mjs` changed under item 13 mid-run, and the report-hygiene deletion of `*-report.md` was undone by their agents. Item 13's commit necessarily swept their in-flight work in. `git status` being clean at spawn time is NOT proof you have the tree to yourself; check whether an earlier item's spawn-log row is still `pending` before assuming. Also: **`quoting` is not in the 0018 `businesses_stage_check` vocabulary** (only `quoted` is) — item 14 owes that migration, and until it lands the quote-handoff template can never fire against real data. Two Importants left open for item 14: (a) notify writes no `email_threads` row, so a `yes`/`no` reply to the batch-approval mail matches no thread and poll forwards it as "command had no thread to apply to" — batch approval is unroutable as built; (b) the `drafted`/`replied`/`quoting` markers are never cleared by any code, so a row that re-enters one of those stages is never notified again (only `call_due` re-arms).

## 2026-08-31 17:25 — dev-team-auto — item 14: end-to-end pipeline test (tests/pipeline.test.mjs)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0008-places, commit `9a6cf8b`
- **What happened:** New `tests/pipeline.test.mjs` (2 tests): one walks a business `sourced→qualified→drafted→sent→replied` through source/qualify/personalize/touch/poll/notify in SCHEDULES order on ONE deps object; one asserts an opt-out mid-path sets `suppressed_at` and that no later read in the same run returns that row. New `supabase/migrations/0019_quoting_stage.sql` adds `quoting` to the stage vocabulary (additive, 0018 untouched, NOT applied), plus a migrations test guarding that 0018 was not edited. Suite 245 → 248 pass / 0 fail / 0 skipped.
- **What worked:** **One in-memory `sql` tagged-template fake that the REAL `lib/db.mjs` runs against, backed by a single store keyed by the 0018 column names.** This is the whole value of the item: no per-job `db` stub exists in the file, so each job is handed the previous job's actual row. The fake filters `selectable_*` reads on `suppressed_at` exactly as the view does and THROWS on any `from businesses` read, so a view bypass fails loudly. `sql.reads` records (phase, ids) per read, which is what makes "no later job selects the suppressed row" an assertion rather than a claim. Unhandled query text throws rather than returning `[]` — a new db.mjs query cannot silently no-op.
- **What failed:** Nothing in the loop. Process defect, mine: I spawned the engineer with `isolation: "worktree"`, which put it in a fresh worktree branched off `outreach-pipeline` that was MISSING jobs/notify, personalize, poll, touch entirely. Caught it from `git worktree list` before the agent had done real work; `TaskStop` refused (an agent cannot stop another's task), so the fix was a `SendMessage` telling it to `EnterWorktree` the correct path. It complied and all work landed in the right tree. **Never pass `isolation` when the item names an existing worktree.**
- **Remember next run:** **Mutation-check with a field the producer's OWN unit test does not assert, or the check proves nothing.** Renaming `facts` in qualify went red in 3 tests including qualify's own — that does not demonstrate cross-job coverage. Renaming `fit` → `fit_level` (producer only) failed ONLY the e2e test (247 pass / 1 fail): that is the real proof the test catches the producer/consumer defect class. Baseline drifted 237 → 245 mid-item because item 12's late review pass committed to this branch (`3a9a13e`) while item 14 ran — the second time two items shared this worktree. Take the baseline from the suite at your actual HEAD, never from the spawn prompt. Two Importants still OPEN, now formally named follow-up items: (a) *"Give notify's mail a thread so yes/no replies land"* — the approval mail is ONE message covering N businesses but `email_threads.message_id` is a PK with a single `business_id`, so the batch cannot be recorded at all; needs per-business approval mail or a mapping table, a design decision not a patch; (b) *"Re-arm the notify dedupe key on stage re-entry"* — `replied → approved → replied` reuses key `id:replied` and is silently never notified again; one-line fix in `notifyKey` (fold `updated_at` in, as `call_due` folds `next_touch_at`) but it changes item 13's tested contract and trades a silent miss for occasional duplicate internal mail. **New Critical the e2e test surfaced: `NOTIFY_ALLOWED_RECIPIENTS` serves two purposes** — it gates who `touch` may mail AND is the recipient list `notify`/`poll` forward internal mail to. Adding prospects to go live means every internal call/meeting/approve mail, and every forwarded reply including a prospect's own opt-out, is delivered to prospects, and `poll.isAllowedSender` would then honour teammate commands (`won 2400`, `stop`) from that prospect. Needs a separate variable for internal recipients before any live pilot. Also: `personalize` has no cron cell and no `SCHEDULES` entry, so `qualified` rows never become `drafted` in production and the sender's buffer stays empty forever.

## 2026-09-01 16:45 — dev-team-auto — item 15: headless-skill runner + os push helper
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0015-skills, commit `1c6e8f9`
- **What happened:** New `lib/skills.mjs` (`runSkill({command,cwd,run,model,timeout})` — claudeClient's exact CLI invocation plus `cwd`, parses only `WROTE:`/`REPO:`/`DRYRUN:` out of the `--output-format json` envelope's `result` field, rethrows naming the command on non-zero exit) and `lib/osrepo.mjs` (`commitAndPush({exec,dir,paths,message,dryRun})` — add → commit → pull --rebase → push, `dryRun` defaults to **true**, returns `{dryRun, commands}`). `lib/claude.mjs` now exports its `run`. `buildDeps` gains `osDir`/`runSkill`/`commitAndPush` under `if (env.OS_DIR)`, with `dryRun` bound from `deps.dryRun`. `clock.yml` gained "Wire ~/os skills and git identity" between the os checkout and Run job. Suite 266 → 273 pass / 0 fail / 0 skipped.
- **What worked:** Injecting `dryRun` as a parameter defaulting to `true` rather than reading `process.env` inside the module — an omitted flag cannot push, and the test asserts that default directly. Testing the invocation argv (asserting `--bare` is absent) turns the 2026-07-09 learning into an enforced contract instead of a comment. Five mutation checks all went RED: pull/push swapped (3 fail), dry-run short-circuit removed (2 fail), `--bare` added (1 fail), clock.yml step deleted (1 fail), rethrow stripped of the command name (1 fail); every file restored byte-identical from a `cp` backup.
- **What failed:** Nothing in the loop. A stale `pending` spawn-log row for this same item from an earlier aborted attempt was found in `~/os/outputs/dev-team-spawn-log.md` and removed.
- **Remember next run:** **`Check out ~/os` in clock.yml sets `persist-credentials: false`, so a live `commitAndPush` has no credentials and will fail at `git push`.** Whoever wires the first job that actually pushes to ~/os (items 16–20) must add a credential path — a PAT-authenticated remote URL or `persist-credentials: true` on that checkout — or the push half is dead on arrival in CI. Two smaller live-path notes: `runSkill`'s 600s default timeout exceeds clock.yml's `timeout-minutes: 10`, so the workflow kills the job before the timeout fires; and `maxBuffer` is 8MB, which a chatty skill run could exceed. Consumers of `runSkill` must expect `dryrun` to be an **array** and `repo` a scalar (last `REPO:` line wins) — LANE.md's `{ wrote: [], repo, dryrun }` does not say which, and a consumer assuming a string `dryrun` is exactly the item-8 producer/consumer defect family.

## 2026-09-01 16:50 — dev-team-auto — item 16: Remove approval loop, migration 0022
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — item/0015-skills, commit 388733b
- **What happened:** `notify` dropped `drafted` from `NOTIFY_STAGES` and lost `approvalEmail` entirely (the batch special-case in `run()` collapsed to the plain per-row map, and `result` no longer carries a `drafted` key); `poll` dropped the `yes` parse rule and its dead `yes → approved` branch, so `yes` now falls through to the unknown-command forward. `supabase/migrations/0022_pipeline_v2.sql` authored as a file only. 273 → 280 pass, zero fail.
- **What worked:** Pre-auditing the blast radius before spawning — `grep -rl approv .` found the whole surface (two test files, `jobs/run.mjs`'s comment) and the spawn prompt named each one, so nothing was missed and no passing test was deleted. Both negative criteria were written against the real `run(deps)`/`poll(deps)` entry points with fakes, and the poll test loops `['yes','yes\nlooks good','Yes.']`. Four independent orchestrator mutation checks (re-add the `yes` rule; re-add `drafted` to `NOTIFY_STAGES`; drop `onboarded` from 0022's CHECK; drop `os_slug` from 0022) each went RED, then restored from a `cp` backup and verified byte-identical.
- **What failed:** none. One prose error in the spawn prompt: I told the engineer `quoted` entered the CHECK at 0019 — it has been there since 0018, and 0019 only added `quoting`. The engineer caught it and wrote the correct history into 0022's header.
- **Remember next run:**
  - **The stage CHECK at HEAD is now 0022's thirteen:** sourced, qualified, call_due, drafted, approved, sent, replied, quoting, meeting, quoted, won, onboarded, lost. `approved` is dead vocabulary — it is kept only so pre-0022 rows stay valid; **nothing writes it and nothing should**. `quoted` has been present since 0018; `quoting` arrived in 0019; `onboarded` in 0022.
  - **0022 is authored but NOT applied.** Items 17–20 write `businesses.os_slug`, `clients.signed_at`, `clients.contract_path`, `clients.repo_url` and the `onboarded` stage — all of those columns exist only in a file on disk. Any test asserting against them must stay textual/faked; a live run against `knmgyxlrhjxaydliucbs` will fail until a human applies 0022.
  - **There is no temp-DB migration replay test in this repo** — confirmed by grep. `tests/migrations.test.mjs` is textual only: it `readFileSync`s each migration by explicit filename and asserts on the SQL text. A new migration is "enumerated" by adding a `--- NNNN ---` block there; there is no glob to fall back on, so a migration with no block is silently uncovered.
  - **`yes` is no longer a command anywhere.** `parseCommand` returns null for it; the honoured set is `no`, `no answer`, `stop`, `won <amount>`, `notes`. Ordering in `parseCommand` still matters: `no answer` is tested before `no`.
  - Still open, still not fixed: the `replied`/`quoting` dedupe markers never clear, so a row re-entering those stages is never re-notified. Only `call_due` re-arms, via `next_touch_at` in `notifyKey`. Removing `drafted` shrank the blast radius but did not fix the mechanism.
  - `os_slug` has **no FK** to `clients.slug` on purpose — the funnel row is slugged at repo-generation time, before the clients row exists. Item 20 must not assume the FK enforces write order.

## 2026-09-01 17:20 — dev-team-auto — item 17: pitch job on call_due
- **Outcome:** DONE — 1 attempt — caution: no (dropped by Nate 2026-09-01, hand-verified) — team: dt-engineer (opus/medium) — item/0015-skills, commit `5cb361a`
- **What happened:** New `jobs/pitch.mjs` — filters `businessesByStage(sql,'call_due')` in JS on `research.pitch_path`, claims `os_slug` (kebab of name+town, retry once with the last 6 of `place_id` on pg `23505`), writes `facts.json` + `page-text.txt` to a `mkdtemp` dir, `runSkill({command, cwd: osDir})`, `commitAndPush({paths: wrote, message: 'pitch: <slug>'})`, then writes `research.pitch_path = clients/<slug>/pitch/` and a `pitched` event. `qualify` builds its research JSON once and adds `page_text` capped at exported `PAGE_TEXT_MAX` (20k). `notify` lost `pitchAvailable`/`hasPitch`/`homedir` entirely; the `call_due` body now carries `Pitch folder: <research.pitch_path>` with a "none yet" fallback. Poll cron → `['poll','pitch','quote','onboard','notify']`. 280 → 289 pass / 0 fail / 0 skipped.
- **What worked:** No `jobs/quote.mjs`/`jobs/onboard.mjs` stubs were written — `main()` already returns `{skipped:'not-built'}` on `ERR_MODULE_NOT_FOUND` and the clock test asserts the name list without loading a module, so two dead files were avoided. Ordering the writes as slug-claim → skill → `pitch_path` is what makes "a crash leaves the row re-pitchable" true, and the error test asserts `h.updates.map(u => Object.keys(u.patch))` deep-equals `[['os_slug']]` — that shape assertion, not a stage check, is what proves research was untouched. Four orchestrator mutation checks independent of the engineer's four all went RED: temp file renamed `page-text.txt`→`pagetext.txt` (1 fail), error event kind `error`→`failed` (1 fail), `Pitch folder: ${research.pitch_path}`→`${row.domain}` (1 fail), `'pitch'` dropped from the poll cell (1 fail); all three files restored and `cmp`-verified byte-identical.
- **What failed:** none. One deliberate deviation from the item text, left standing and flagged for Nate: LANE.md says write `research.facts` as the `--facts` JSON; `~/os/skills/pitch/SKILL.md:394` documents that input as the `businesses` row plus `research`, so the engineer writes `{...row, research}` — a superset. A pitch built from 2–5 bare fact strings has no phone, town or trade to write a call script from. This is the item-8 producer/consumer family handled correctly: assert against the real consumer, not the item's prose.
- **Remember next run:**
  - **`commitAndPush` defaults to dry run, and `pitch` writes `research.pitch_path` regardless of whether a push happened.** Guardrail 1 is "one pitch per business, EVER", so a dry-run tick permanently marks the row as pitched with nothing in ~/os. Items 18–20 reuse this shape (`quote_path`, `contract_path`) and inherit the same trap. If a live pilot ever runs with `DRY_RUN` still on, every row burns its one pitch.
  - **The 20-minute poll tick now runs five jobs at `limit=25`, each row one Claude CLI run at `runSkill`'s 600s default, inside `timeout-minutes: 10`.** One slow pitch kills the tick before `notify` runs. Neither timeout was changed; whoever wires the first live tick must cap `pitch`'s limit or shorten the per-call timeout.
  - `clock.yml`'s `persist-credentials: false` on the ~/os checkout is now a REAL blocker, not a theoretical one — `pitch` is the first job that actually calls `commitAndPush`. Untouched by this item.
  - `research` now carries five keys across the pipeline: `facts`, `fit`, `reason` (qualify), `page_text` (qualify, item 17), `draft` (personalize), `pitch_path` (pitch), plus `owner_name`/`notes` written by poll. Every writer must spread the parsed object, never rebuild it — `pitch` does `{...research, pitch_path}`.
  - `personalize` still has no cron cell and no `SCHEDULES` entry. Item 17 edited `SCHEDULES` and deliberately did not fix it; it is still true that `qualified` rows never become `drafted` in production.

## 2026-09-01 — dev-team-auto — item 18: quote job on notes
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — branch `item/0015-skills`, commit `2915508`
- **What happened:** Built `jobs/quote.mjs` (stage `quoting` + `research.notes` + no `research.quote_path` → notes to a temp `.md` → `/quote <slug> --notes <md> --yes` → push → `clients` insert if absent → `stage = quoted` + `quote_path` → `quoted` event), added `db.clientByBusiness`/`db.insertClient`, and gave `notify` a fourth stage `quoted` with `quoteReadyEmail`. Same commit fixed the dry-run marker defect family: new `pushOrSkip` in `lib/osrepo.mjs` is the only sanctioned way to persist a completion marker, `jobs/pitch.mjs` was retrofitted onto it, and the rule went into CLAUDE.md's Rules block for item 20. Suite 289 → 298 pass, 0 fail, 0 skipped.
- **What worked:** Mutation-checking all four load-bearing behaviours rather than trusting green — flipping `--yes`→`--yeah`, deleting the clients-insert guard, removing the word `signed` from the mail, and making `pushOrSkip` ignore `dryRun` each turned the suite red (the last one red in two tests, one per job). Restoring the mutated files from `cp` backups and `cmp`-verifying byte-identical, never `git checkout --`. The fake DB's `updateBusiness` actually applying the patch to its store is what makes the two-run negative assertions real rather than re-derived.
- **What failed:** none in the loop. The plan under-specified: item 18's spec inherited a defect from item 17 that had to be fixed here for both jobs, which is scope the item never priced.
- **Remember next run:** **The dry-run marker rule is now a project standard** — any job persisting "this is done" after a `commitAndPush` MUST go through `pushOrSkip` (`lib/osrepo.mjs`) and put the marker write, the stage move and the success event behind `if (!push) continue`. Item 20 writes `clients.repo_url` after the same call and must use it. The slug claim deliberately stays AHEAD of that guard in both jobs — the slug must be stable across retries. Existing pitch tests changed contract: `tests/pitch.test.mjs`'s harness default push flipped `{dryRun:true}` → `{dryRun:false}`, because under the new rule a dry-run push marks nothing and every happy-path pitch assertion would otherwise assert a skip; the dry-run path is now asserted explicitly in `tests/quote.test.mjs` for both jobs instead. `notify` now has four stages and a nested ternary picking the template — item 20's `onboarded` stage should turn that into a lookup rather than adding a fifth arm. The `quoted` dedupe marker is never cleared (`ponytail:` comment in `jobs/notify.mjs`), same known-open as `drafted`/`replied`/`quoting`. `docs/JOBS.md` and `docs/NOTIFICATIONS.md` still reference the deleted Next.js `lib/notify.ts` — stale before this item, still stale, worth its own cleanup item.

## 2026-09-01 — dev-team-auto — item 19: signed contract in `poll`
- **Outcome:** DONE — 1 attempt — caution: no (flag removed by Nate 2026-09-01, with a standing order to mutation-check the refusals) — team: dt-engineer opus/medium — branch `item/0015-skills`, commit `3545ed5`
- **What happened:** `parseCommand` gained `signed`; `handleSigned` in `jobs/poll.mjs` files exactly one `application/pdf` part of at most 10 MB to `$OS_DIR/clients/<slug>/contract/<YYYY-MM-DD>-signed.pdf`, pushes it via `pushOrSkip` as `contract: <slug>`, then writes `clients.signed_at`/`contract_path`, `stage = won` and a `signed` event. `toMessage` in `jobs/run.mjs` now exposes `attachments: [{contentType, size, filename, content}]`. `lib/db.mjs` gained `updateClient`. 298 → 310 pass, 0 fail, 0 skipped.
- **What worked:** All four refusals were written as four separate, independently deletable `if`s with a comment saying so — the orchestrator's mutation pass then deleted each one alone and got exactly one named failing test each (`no attachment` / `second attachment` / `text/html` / `11 MB`). The tests build their fixture by calling the REAL `toMessage` rather than hand-rolling a message object, so the new attachment shape is proven producer-to-consumer in the same test, not just against the item's own assumption. Negative assertions read `readdir(osDir)` on a real temp dir instead of asserting a mock was not called.
- **What failed:** Nothing in the loop. One plan/reality mismatch: LANE.md names `lib/mail.mjs` and no such file exists — the IMAP parse is `toMessage`/`drainMessages`/`createImap` in `jobs/run.mjs`. Extended in place; a move would have touched `tests/poll.test.mjs`, `tests/run-imap.test.mjs` and `tests/pipeline.test.mjs` for no behavioural gain.
- **Remember next run:** **Mutate the guard by rewriting its condition to `false`, not by changing a value it reads** — that is what proves the check itself is load-bearing. The size test allocates a literal `Buffer.alloc(11 * 1024 * 1024)` and never imports `MAX_CONTRACT_BYTES`, so widening the constant to 12 MB also goes red; item 11's vacuous-assertion trap is closed here. Two live caveats carried forward, neither introduced by this item: (1) `jobs/run.mjs` builds `deps.commitAndPush` with **no `exec`**, so the first real (non-dry-run) push in `pitch`, `quote` OR `poll` calls `undefined` — this is now a three-job blocker and item 20 or a fix item should close it; (2) under dry run the PDF is written into `$OS_DIR` before `pushOrSkip` returns null, leaving an uncommitted file in the clone (idempotent — same path, message left unseen). Also: the content type is the sender's claim only; there is no `%PDF` magic-byte sniff. `docs/JOBS.md`/`docs/NOTIFICATIONS.md` still reference the deleted Next.js `lib/notify.ts` — stale before this run, still untouched.

## 2026-09-01 17:30 — dev-team-auto — item 20: onboard job
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/medium) — `item/0015-skills`, `6be56ad`
- **What happened:** `jobs/onboard.mjs` built on the pitch/quote shape (parseResearch + claimSlug reused, pushOrSkip mandatory). `notify` gained `onboarded` + its own single-address notifier and the four-arm ternary became a `TEMPLATES` lookup. Also fixed the live `commitAndPush` wiring bug (no `exec`) that broke pitch/quote/poll's first real push. 310 → 327 tests, 0 fail.
- **What worked:** Reading the real `~/.claude/skills/*/SKILL.md` headless contracts BEFORE writing the fake `runSkill` fixture — `/new-client-repo --dry-run` prints only a `DRYRUN:` line and no `REPO:`/`WROTE:`, which is what made the dry-run design obvious. Item 19's separate-independently-deletable-`if` pattern made all four mutation checks single-line and unambiguous.
- **What failed:** none.
- **Remember next run:**
  - **`/new-client-repo` has a real, irreversible side effect (`gh repo create`), unlike `/pitch` and `/quote`.** `pushOrSkip` cannot undo it. Any future job wrapping a repo-creating or otherwise irreversible skill must branch on `dryRun` BEFORE the call and use the skill's own `--dry-run`, not run it and skip the marker afterwards — otherwise every dry tick creates another repo.
  - **`createNotifier` allow-lists against `internalRecipients`, so a second recipient list needs a second notifier.** Do not try to route a non-internal address through the shared `send`; build `createNotifier({ internalRecipients: [theOneAddress] })`. There are now three lists in `buildDeps`: `allowedRecipients` (prospects), `internalRecipients` (internal humans + command senders), `onboardRecipient` (exactly one).
  - **`buildDeps` spreads caller `opts` LAST**, so a test that supplies its own `exec`/`dir` masks a missing default. `commitDefaults(env, dryRun)` is now exported from `jobs/run.mjs` specifically so the wiring is assertable without executing a push — assert against it, not against a call you parameterise.
  - `notify`'s template choice is now `TEMPLATES[stage]` — a new stage that forgets its entry throws instead of silently mailing the quote-ready body (the old ternary's last arm was the default).
  - Still open, untouched: the `drafted`/`replied`/`quoting`/`quoted`/`onboarded` notify dedupe markers are never cleared; `docs/JOBS.md` and `docs/NOTIFICATIONS.md` still reference the deleted `lib/notify.ts`.
