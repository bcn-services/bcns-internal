# Progress

LANE.md is the contract; this tracks where we are in it. If they disagree,
LANE.md wins for scope.

**Current position**
- Status 2026-09-11: bcns moved off the per-client-repo model to a shared
  data-layer platform (one shared app, `client_id`-scoped), so item 20's
  `/new-client-repo` call is superseded. `jobs/onboard.mjs` now runs `/intake`
  only — no repo, no `clients.repo_url` write. The idempotency read moved
  from `client.repo_url` to `research.intake_checklist_path`, same shape as
  `quote.mjs`'s `research.quote_path` gate; the dry-run guard collapsed to
  the same pre-skill-call shape `quote.mjs` and `pitch.mjs` use, since
  `/intake` (unlike `gh repo create`) is a reversible file write. `notify.mjs`'s
  onboarded-stage mail dropped its `Repo:` line. `clients.repo_url` stays in
  the schema (harmless, nullable, still correct for the legacy custom-build
  clients like TA and l2detailz) but new onboards never set it. Item 20's
  original spec in LANE.md and its `LANE_PROGRESS.md` entry below are left as
  the historical record of what was originally built. 434 tests, none
  failing.
- Status 2026-09-07 (overnight autonomous run, branches `heartbeat-chain` and `alert-fixer`): the Monday clock failure is explained and fixed — the heartbeat job was never scheduled, so the commit step had nothing to add; it now runs last on the Monday tick and a manual dispatch proved the commit lands. Alert triage grew from "file it" to "fix it": an alerts@ mail is parsed (GitHub Actions, Sentry, UptimeRobot, anything else), mapped to a repo, cloned, handed to Claude to reproduce and fix, and pushed as a draft PR carrying the fix or a TRIAGE.md diagnosis. Cap, dedupe and never-merge stay. Gmail's pipeline filter now also routes alerts@. 390 tests, none failing.
- Next: merge PR #9 (heartbeat) and PR #10 (`alert-fixer`); `ALERT_REPO` and `ALERT_REPO_MAP` repo variables are set; point GitHub, Sentry and UptimeRobot notifications at alerts@bcn-services.com; decide on `DRY_RUN`. Until DRY_RUN is off the runner only logs `would_triage`.
- Blockers: none in code. The runner's `OS_TOKEN` must be able to push a branch and open a PR on every mapped repo; unverified from a session.

- Status 2026-09-04 (overnight live run, branch `e2e-live`): the pipeline has now been proven end to end with real mail, twice locally and once on GitHub Actions. A fake prospect went drafted → sent → replied → notes → quoting → quoted → signed → won → onboarded, ending in a private client repo and a "your turn" handover mail; stop replies suppress, a call-due lead gets a pitch folder and a notice naming it, a second attachment on a signed reply is refused, a lead that stops replying gets exactly two bumps and then a call task. Sourcing, qualification and personalization all ran live on the runner with the voice gate on. Ten fixes landed on the way, none of them in the schema: no-domain leads are calling leads, dashes are stripped from pitch and quote files, drafts read as sentences, onboarding on the runner has the token the repo skill needs, a quiet notify tick still logs. Migrations 0022-0025 are applied. 368 tests, none failing.
- Next: merge the `e2e-live` PR, then the only human work is the second sending domain: give the sending seat an `outreach@trybcns.com` send-as alias, publish SPF, DKIM and DMARC for trybcns.com at Namecheap, and flip the two `mailboxes` rows (steps in docs/NOTIFICATIONS.md). The runner's Claude token is a short-lived copy of the local login; run `claude setup-token` for one that lasts. `DRY_RUN` is back to true.
- Blockers: none in code. The trybcns.com flip cannot be done from a session (admin console password re-challenge, Namecheap blocked for the browser extension, alias verification mails an outside address).
- Status 2026-09-01 (autonomous run, items 15-20): all six done, none blocked, each in a single attempt. The pipeline is now complete end to end on paper — pitch, quote, signed-contract filing and onboarding all exist and all push to ~/os behind one shared guard. 334 tests pass, none failing, up from 266 at the start of the run. Stopped at the marker as intended; item 21 untouched.
- Next: nothing in the code blocks a pilot. What blocks it is human: apply migration 0022 by hand (it is authored and unapplied, and every new column this round writes to lives in it), set `ONBOARD_NOTIFY_TO`, and decide whether to flip `DRY_RUN=false` in `clock.yml`. Until that flip, every job stops before its side effect and logs a `skipped` event saying what it would have done — which is the correct place to watch it from for a few days first.
- Lane acceptance 2026-09-01: no `## Objective` block and no `Lane done when:` criteria exist in LANE.md, so the check ran against the `## Conventions this round establishes` bullets plus the 30 `done when:` criteria of items 15-20. All 30 met, each by a test driving the job's real `run()` rather than re-deriving the property from a mock. Five of six conventions met. The sixth was fixed rather than recorded: jobs tested the `OS_DIR` variable instead of the directory, so a missing ~/os clone would have thrown where the convention requires a `skipped` event.
- Four pre-merge fixes applied after that review, commit `65c177f`. The one worth knowing: pitch and quote spent the paid Claude call BEFORE the dry-run guard and then left the row unmarked, so a single `call_due` row would have burned roughly 37 model runs a day, forever, producing nothing. Onboard already had the right shape and the other two now match it. Also fixed: a skill that wrote nothing produced a permanent error loop, and the handover mail was unreachable whenever the internal recipient list was empty.
- Caution flags, settled 2026-09-01 by Nate mid-run: items 17-20 all carried `caution: true` and all four lost it, on the grounds that the pitch, quote, contract and onboard artefacts are each visible enough to check by hand. Every item this round therefore ran `dt-engineer` alone. This changed who verifies, not the bar: each item's orchestrator mutation-checked every guard itself, breaking the check's own condition and confirming the suite went red, 22 such checks across the round. Worth keeping in mind for item 21 — the refusal paths in item 19 were the reason to hesitate, since a refusal that never fires looks exactly like a clean run, and they were the ones proven hardest.
- Status 2026-09-01: items 1–14 done and on `main` (PR #3); PR #4 `pipeline-fixes` (cap reset, qualify crash fix, migrations 0019–0021) open, unmerged. Branch `pipeline-v2` carries the rewritten items 15–20 for the no-approval design: skill runner + os push helper, approval removal + migration 0022, pitch job, quote job, signed-PDF handling, onboard job. Stop marker sits after item 20. Item 21 (alert triage) waits below it. The paired `~/os` work (four skills' headless flags) is its own LANE.md on the `headless-skills` branch of bcns-os and is NOT run from here.
- Next: `/dev-team-auto` on `pipeline-v2` from item 18 (quote job). The "notify mail has no thread, so yes/no replies do not land" finding below is moot once item 16 removes the approval mail.
- Status: items 1-14 of 16 done. Items 8-14 were built in one autonomous `/dev-team-auto` pass on 2026-08-31, which stopped at the marker as intended. 248 tests pass, 0 fail, 0 skipped (up from a 75-test floor at the start of the round). The whole pipeline now exists end to end and one test proves the six jobs agree on what they hand each other. NOTHING HAS BEEN SENT and nothing may be until the items below are settled. Migration 0019 (the `quoting` stage) is written but NOT applied.
- Next: four findings from the run now sit at the top of LANE.md under "needs an item" and must be settled BEFORE any live pilot. In priority order: (1) `NOTIFY_ALLOWED_RECIPIENTS` does double duty as both the who-may-we-mail gate and the where-do-we-forward-internal-mail list — adding a prospect to go live would deliver internal mail to prospects and let them issue commands; split it first. (2) `personalize` has no cron cell, so the pipeline stalls one step before its first send. (3) The per-mailbox daily counter is never reset, so today's allowance behaves as a lifetime allowance. (4) notify's mail carries no thread, so `yes`/`no` replies are forwarded rather than applied. Then items 15/16 (quote handoff, alert triage), which sit below the stop marker.
- LANE.md pre-run edits 2026-08-31, before any auto pass: (1) the forbidden-list line "Sending any email to any address, real or test. This round has no send path" would have made an engineer mark item 11 `blocked` — it now forbids EXECUTING a send while allowing a send path to be built; (2) item 11's allow-list guardrail said addresses outside `NOTIFY_ALLOWED_RECIPIENTS` are refused "while `DRY_RUN` is on", which guards nothing, since `DRY_RUN` on opens no SMTP at all — grep confirmed no code anywhere reads the variable, so with `DRY_RUN` off item 11 as specified would have mailed real leads. The gate is now stated as independent of `DRY_RUN` and carries its own done-when; (3) item 11 gained a done-when for the `multipart/alternative` signature parts, which only LANE_PROGRESS.md had recorded; (4) the `gh` 404 gotcha moved into Global rules so subagents inherit it; (5) the stale Status preamble was refreshed.
- Mail setup CLEARED 2026-08-31 (walked through the console in the work Chrome profile, each step verified by read-back): leaked app password `bcns-outreach` revoked; `bot@bcn-services.com` created as an alias on Nate's seat (aliases now `outreach@`, `bot@`, `alerts@`, `dmarc@`); `bot@` added as a Gmail send-as and `outreach@send…` flipped to "treat as an alias", with replies going out from the address the message was sent to; the `pipeline` filter widened to `to:(outreach@send.bcn-services.com OR bot@bcn-services.com)` → Skip Inbox, label `pipeline`, never spam; ONE new app password minted and written to BOTH `SMTP_PASS` (overwriting the stale one) and `IMAP_PASS`; `NOTIFY_ALLOWED_RECIPIENTS` set as a repo VARIABLE to `nseluga@bcn-services.com,bchung@bcn-services.com`; Nate's REAL Gmail signature committed verbatim as `lib/signature.html` (exact markup lifted from the Gmail settings editor: blue left rule, monospace BCNS wordmark, mailto + site link, no images — there was never a logo) with `lib/signature.txt` as the matching text/plain part. Item 10 reads these two files; there is no `NATE_SIGNATURE` secret. **Item 11 must send `multipart/alternative` with BOTH parts** — an HTML-only send is a spam signal on its own. Nate chose the full signature over a trimmed one on 2026-08-31 after being shown the deliverability argument (new subdomain, zero reputation, link count during warming); recorded as his decision, do not re-litigate. Watch the Postmaster spam rate over the first warming weeks. MX/SPF/DKIM/DMARC live on both domains. Nothing mail-related now blocks the lane.
- `gh` gotcha, still live: the `read:packages` PAT exported as `GITHUB_TOKEN` in `~/.zprofile` shadows the keyring account and 404s on this repo instead of 403ing. Use `GITHUB_TOKEN= gh ...` for anything repo-scoped.
- Open, not blocking: `send.bcn-services.com` is a USER ALIAS domain (auto-mirrors every seat address, which is why it cannot host an explicitly-created alias). The primary `bcn-services.com` shows email setup "Action needed" in Manage domains; the `send.` row is clean. Look at it after the first end-to-end run.
- Gap closed 2026-08-31: an end-to-end item now sits directly above the stop marker as item 14, so the auto pass builds it. It drives `run.mjs`'s deps through source -> qualify -> personalize -> touch -> poll -> notify on the existing fakes, and stays pure. Items 8-13 land unit tests only; nothing else ever proves the jobs agree on the row shapes they hand each other.
- Team sizing revised 2026-08-31: `caution: true` now sits on items 3 and 12 only. Items 1, 2 and 6 lost it retrospectively (already done, no effect on any future run); the stop marker moved to after item 13, so an auto pass covers items 8-13 (the full loop) and leaves 14/15 for later; item 11 lost caution deliberately — the first sends go only to Nate via `NOTIFY_ALLOWED_RECIPIENTS`, and he reviews before any real client is mailed.
- Live-system note: the crons are firing but nothing does work yet. `poll` and `touch` do not exist and are skipped cleanly; `source` no-ops because `run.mjs` does not inject `places`/`readBudget`; `qualify` is in no schedule. Nothing sends.
- Decision closed 2026-08-31: `~/os` reaches the runner as a clone of `bcn-services/bcns-os`, wired into `clock.yml` behind the `OS_REPO` variable and the `OS_TOKEN` fine-grained PAT (the org disables deploy keys). Both are set on the repo. The clone is optional by design, so items 8-14 build the same way whether or not it lands; `authcheck` reports its state on dispatch.
- Caution flags, settled 2026-08-31 by Nate mid-run: item 12 kept `caution: true` (full engineer+QA+review); items 11 and 13 dropped it and ran engineer-only. The reasoning was that Nate reviews every item himself and tests with mail sent to his own address, so a loud local failure is caught — but a false-negative opt-out is not, which is why the read-only poller kept the full team. In place of a QA agent, items 11, 13 and 14 each had their safety gates mutation-checked: the gate was deliberately broken and the suite confirmed to go RED. That caught a vacuous test on item 11 that would otherwise have passed.
- Item 22 (2026-09-03), solo engineer run on branch `item-22`, `caution: true` retained: per-mailbox SMTP transport built and self-verified against all four done-when criteria (no separate QA gate this run). All three mandated mutation checks performed, confirmed to fail, then reverted byte-identical: (1) dropped the address-equality guard in `resolveMailboxAuth` — the "never fall back" tests went red (7/9); (2) reinserted a literal `@bcn-services.com` credential default — the literal-scan test went red (8/9); (3) leaked the transport's own `auth.pass` into a `sent` event's detail — the leak test went red (22/23). 345/345 tests pass on the clean, reverted state; lint clean.
- Last updated: 2026-09-07 (overnight run: heartbeat fix, alert fixer). Previous: 2026-09-04 (overnight live end-to-end run on `e2e-live`, verified locally and on the runner). Previous: 2026-09-03 (item 22, per-mailbox SMTP transport, solo engineer run). Previous: 2026-09-01, autonomous run over items 15-20 complete; stopped at the marker; 334/334 green; lane acceptance ACCEPT, all 30 criteria met; four pre-merge fixes applied, one of them a live cost bug. Previous: 2026-08-31, items 8-14.

## Round 2 — outreach pipeline (in progress)

## 2026-09-07 — heartbeat + alert fixer

| Item | Status |
|------|--------|
| Build alert triage (item 21) | done — an alerts@ mail becomes a draft PR, capped at three a day, one per incident. |
| Parse alerts@ mail into one shape | done — GitHub Actions, Sentry and UptimeRobot mail are recognised and mapped to the repo they belong to. |
| Fix the alert, not just file it | done — the repo is cloned, Claude attempts a tested fix, and the branch carries either the fix or a written diagnosis. |
| Wire parser and fixer into poll and the clock | done — the pipeline opens the draft PR on the right repo; unmapped alerts land on the fallback repo, monitor recoveries open nothing. |
| Live proof (DRY_RUN=false, local) | done — the real 45e294b failure mail forwarded to alerts@ produced draft PR #11 with the same heartbeat fix as PR #9 plus the two updated tests; closed as a duplicate. Same-seat forwards carry no Delivered-To and arrive already read: poll now routes on the To: line, and the mail had to be marked unread by hand. |
| Monday clock tick failing at "Commit heartbeat" | done — the heartbeat file is now written on the Monday tick and the commit step tolerates a skipped run. |

| Item | Status |
|------|--------|
| 1. Strip the repo to a headless jobs runner | done — the old dashboard app is gone and the repo is now just a jobs runner: no web pages, no server, no Next.js. Dependencies are down to the four the pipeline actually needs, `pnpm test` runs, and every migration file was left exactly as it was. |
| 2. Two schema migrations (files only) | done — two SQL files are written and ready for a human to apply: one clears out the old dashboard tables, the other creates the pipeline's own six tables. Nobody can be mailed twice, an opt-out can never be undone, and every job reads leads through a view that hides opted-out people automatically. Nothing was applied to the live database. |
| 3. lib/db.mjs — the only SQL writer | done — all database access now runs through one module, and it refuses at runtime to look up a lead in a way that could show someone who opted out. Opting out is one-way: asking twice does not move the date, and nothing in the code can clear it. The module never opens its own connection. |
| 4. Clock workflow + job dispatcher + heartbeat | done — the pipeline now has a clock: GitHub runs it every twenty minutes on weekday daytimes, once each weekday afternoon, and once on Monday mornings, and you can also run any job by hand from the Actions tab. Two runs of the same schedule can never overlap. A weekly dated file gets committed so GitHub never switches the schedule off for inactivity, and a failing job fails the run loudly instead of looking green. A hand-run `authcheck` job proves the keyless Google sign-in works. |
| 5. Weekly sourcing over a search grid | done — every Monday the pipeline searches one trade-and-town pair from a fixed list of 64 (eight trades across seven Connecticut towns and Providence) and adds the businesses it has not seen before. It checks the Google budget before spending anything and stops rather than guessing if it cannot read it. When a search comes back more than ninety percent already-known businesses the pair is marked used up and never searched again. It cannot search a town or trade that is not on the list. |
| 6. Qualification — fetch + one Claude call | done — each newly found business gets its website read and summarised into three to five real facts about it, with an email address picked up only if the site actually publishes one. An address the model made up is thrown away: if it does not appear on the page it does not get written, so nobody is mailed at a guessed address. A business with no published address becomes a calling lead with its phone number intact, not a discard. A site that will not load leaves the record exactly as it was and logs the failure. Claude runs through the Claude Code CLI on the existing subscription, so none of this bills the API. |
| 7. Email verification before any send | done (merged to `main` 2026-08-31) — before any address is ever mailed, the pipeline checks it is real. First it asks whether the domain can receive mail at all; if it cannot, nothing is contacted and no connection is opened. Only a domain that passes gets a short conversation with its mail server, which asks "would you accept mail for this person?" and then hangs up. It can never send a message: the command that begins a message body is blocked in code, and the tests fail if that block is removed. A "maybe" answer — a server that is rate-limiting, or one that accepts everything — is recorded as unknown and never counted as a yes. |
| 8. Wire sourcing to the real world — Places client + budget reader | done — Monday's sourcing run can now actually reach Google. The pipeline asks Google Places for businesses in the chosen trade-and-town pair using the keyless sign-in already proven working, with no API key anywhere, and it reads how much of the month's Google budget is left before it spends a penny — refusing to guess if that figure will not read. It also found and fixed a naming mismatch left over from an earlier item that would have made the first real run quietly find businesses and save none of them. |
| 9. Wire qualification into the clock | done — the Monday run is now a two-step tick: it finds businesses, then immediately reads and qualifies them, in that order, sharing one set of connections. Each newly qualified address is checked for real before it is kept: an address that fails becomes a calling lead with its phone number intact and its email cleared, while an inconclusive answer leaves the lead as it was rather than guessing. If anything the job needs is missing it records why it stood down instead of crashing the run. Known limit: GitHub's runners usually block the port the address check dials, so in production that check will most often answer "cannot tell" until it is moved to a relay. |
| 10. Personalization — the cold email itself | done — the pipeline now writes the actual cold email. One model call per business fills the single personal sentence; the rest of the letter is fixed wording copied from the outreach skill and checked character-for-character against it, so every recipient gets the same email apart from that one sentence and the name slots. A business with fewer than three real facts about it is skipped before the call is spent rather than mailed thin copy. A draft that somehow contains a web link, a price, or a demo claim is thrown away and logged instead of queued. It writes at most 25 drafts ahead of the sender so unsent copy cannot pile up. |
| 11. The sender — `touch` job, round-robin, 2 bumps | done — the pipeline can now send. Each weekday afternoon it works through the waiting drafts and the follow-ups that have come due, spreading them across the hour and across mailboxes so no mailbox goes over its daily allowance, with a ramp that keeps a new mailbox sending only a little at first. Each email records itself and the follow-up date in the same single write, so a lead can never be counted as mailed without the record of it. Follow-ups reply inside the original thread; after the second one with no answer the lead becomes a calling task rather than getting a third email. Nobody who opted out or already replied is ever selected. The safety gate holds: with dry-run on nothing opens a connection at all, and with it off the only addresses reachable are the two on the allow-list — checked before any connection opens, and proven by deliberately breaking the check and confirming the tests fail. **Known limit, needs its own item: the per-mailbox counter is never reset, so today's allowance currently behaves as a lifetime allowance and sending will stop once it is reached.** |
| 12. Poller and reply parser, incl. Brandon's commands | done — the pipeline can now read its own replies. Every twenty minutes it checks the mailbox, matches each message back to the business it belongs to, and acts on it. Anyone asking to be left alone is taken off the list first, before anything else happens and before the model is ever consulted — so even if the model is broken, missing, or throws an error, the opt-out still lands. That is checked by deliberately breaking the model and confirming the person is still suppressed. It reads the request whether it arrives in the subject line, in plain text, or in an email that is all formatting and pictures with no plain text at all — including when the words are broken up by styling or written with a curly apostrophe. Our own "Reply stop" footer quoted back at us does not count, and neither does an out-of-office, which changes nothing. Anything it cannot confidently read is forwarded to a human rather than guessed at. Brandon's and Nate's one-word commands — yes, no, no answer, stop, won with an amount, and notes — are obeyed only from their own addresses; the identical words from a stranger change nothing at all and are forwarded instead. Verified by deliberately breaking each safety check in turn and confirming the tests fail. An interrupted run repeats a message rather than losing it, on purpose: a duplicate is visible, a dropped opt-out never would be. |
| 13. The notification emails | done — you and Brandon now get told what needs you, at the end of every twenty-minute check. Four kinds of message: tomorrow's drafts for you to approve with a one-word yes or no, a call task with the phone number and the facts when a lead has stopped answering email, a heads-up when someone actually replies, and Brandon's quote handoff. Each lead produces each kind of message once, not once every twenty minutes — proven by running it twice and confirming the second run sends nothing. Mail can only ever reach the two allow-listed addresses; a prospect can never be a recipient, and the refusal happens before any connection is opened. Both of those were verified by deliberately breaking them and confirming the tests fail. Notifying never moves a lead's stage — it only reports what the sender and the poller already wrote. **Three known limits, all for the end-to-end item to settle: the database does not yet recognise the `quoting` stage, so the quote-handoff message cannot fire on real data until a small migration adds it; replying yes or no to the approval email currently matches no conversation, so it is forwarded to a human instead of applied; and apart from the call task, nothing clears the once-per-lead marker, so a lead that returns to an earlier stage would not be re-announced.** |
| 14. End-to-end test — one business through every job in order | done — one test now walks a single business the whole way through: found, checked and given an address, drafted, sent, its reply read, and reported to you — each job handed the real output of the one before it, against the real database layer rather than a stand-in. This is the item that proves the six jobs actually agree on the information they pass each other, and it earned its keep: renaming a single field in one job breaks this test and nothing else, which is exactly the kind of mismatch that would otherwise have looked fine in every other test and failed only on the first real Monday. A second check confirms that someone who opts out mid-way is removed and that no later job that same run picks them up again. A new migration adds the missing `quoting` stage the database did not recognise; it is written but not applied — that is yours to run. |
| 15. Headless-skill runner + os push helper | done — jobs can now run an ~/os skill headlessly and read back only the three declared lines (WROTE/REPO/DRYRUN); nothing else Claude prints is trusted or executed. The ~/os push helper commits and pushes what a skill wrote, in a fixed add → commit → pull --rebase → push order, never force, never against this repo, and under dry run it executes nothing at all and returns the commands it would have run. clock.yml now symlinks the cloned skills into place and gives the clone its own bot identity. Nothing calls either helper yet — item 15 builds the capability. |
| 16. Remove approval loop, migration 0022 | done — nobody is asked to approve a draft any more. `notify` no longer reads the `drafted` stage at all, so the daily batch approval email is gone; `touch` still sends out of the `drafted` buffer at 14:00 unchanged. `yes` is no longer a teammate command: a `yes` reply, even from an allow-listed sender on a real thread, is now forwarded to a human as an unrecognised first line and moves no stage. `no`, `no answer`, `stop`, `won` and `notes` are untouched. This also closes a known hole — the approval mail wrote no thread row, so its own `yes`/`no` replies could never be matched back to a business. Migration 0022 is authored as a file and NOT applied: it gives a funnel row an `os_slug`, gives a client `signed_at`, `contract_path` and `repo_url`, and adds the `onboarded` stage. `approved` stays in the constraint so rows written before today remain valid; nothing writes it any more. |
| 17. Pitch job on call_due | done — every business that goes to the phone now gets a sales pitch built for it before anyone dials, once and once only. The job runs on the twenty-minute tick, picks up each `call_due` row that has no pitch yet, gives the business a slug in ~/os (its name and town, with the tail of the Google place id appended if that slug is already taken), hands the `/pitch` skill the facts and the page text `qualify` already stored, and pushes whatever the skill wrote back to ~/os under a `pitch: <slug>` commit. It never fetches a website — a site that has since gone dark still produces a pitch, because everything it needs was saved months earlier. A row that already has a pitch is skipped forever. A failed skill run records the exact command and its stderr and leaves the row's stage and research exactly as they were, so the next tick simply tries again. The call-task email now names the pitch folder instead of telling a teammate to run `/pitch` by hand, and the check for whether ~/os is on the machine is gone. `qualify` now keeps the page text it read on the row, capped at 20k characters. Two live caveats: migration 0022 is still unapplied, so writing the slug fails against the real database until a human applies it, and clock.yml's ~/os checkout still keeps no credentials, so the push half cannot yet run in CI. |
| 18. Quote job on notes | done — a business Brandon has taken to `quoting` and left notes on now gets a real quote drafted for it on the twenty-minute tick. The job writes his notes to a temp `.md`, runs the `/quote` skill headlessly against that file alone (nothing is asked, nothing is invented), pushes whatever the skill wrote back to ~/os under a `quote: <slug>` commit, opens the client's row in the paying roster if one is not there yet — with no money figure guessed, only the name and the funnel row it came from — moves the business to `quoted` and records where the quote lives. A row with no notes waits rather than quoting from nothing; a second run on the same row is a no-op that says so; a failed skill run keeps the row exactly where it was so the next tick retries. Nothing here mails the prospect. A fourth internal email joins the three: "Quote ready for <name>", to the internal allow-list only, naming the quote folder and asking the human to send it themselves and reply `signed` with the signed PDF when it comes back. It also fixed a defect that would have killed the pipeline on its first real tick: because dry run is the default, both the pitch and the quote job used to mark a row finished while pushing nothing at all, and the row filters then skipped those rows forever. Marking a row done is now allowed only after a push actually happened — one shared rule, both jobs, written into CLAUDE.md so the onboard job inherits it. Two live caveats unchanged: migration 0022 is still unapplied, and clock.yml's ~/os checkout still keeps no credentials. Known limit: a row re-entering `quoted` is never re-notified, the same never-cleared dedupe the other stages have. |
| 19. Signed PDF → contract file → won | done — replying `signed` with the countersigned PDF attached now files it and closes the deal. From one of the two internal addresses, on a thread whose business is at `quoted`, with exactly one PDF of at most 10 MB: the file is written to `clients/<slug>/contract/<date>-signed.pdf` in ~/os, pushed under a `contract: <slug>` commit, and only then is the client's signing date and contract location recorded and the business moved to won. Nothing is marked done on a dry run — the file stays put, the message stays unread, and the next live tick does it for real. Four separate refusals guard the write, and each was verified by deliberately deleting it and confirming the tests go red on its own test: a `signed` with no file attached is forwarded to a human and changes nothing (a forgotten attachment is a slip, not an error); a second attachment, anything that is not a PDF, and anything over 10 MB each refuse the whole message with a forward and a logged error, and nothing reaches disk. The size limit is proven against a real 11 MB file rather than the setting itself, so raising the limit breaks the test instead of quietly moving with it. The mailbox reader now hands attachments (type, size, bytes) to the poller for the first time, with the content type normalised once at the parse so casing and parameters cannot slip a `.html` through. `signed` from a stranger changes nothing, and `won <amount>` is untouched. Deviation from the plan's wording: there is no `lib/mail.mjs` in this repo — the IMAP parse lives in `jobs/run.mjs` and was extended there rather than moved. |
| 20. Onboard job → new-client-repo + intake → mail Nate | done — a signed client now gets a repo and an intake folder without anyone doing it by hand. Every twenty minutes the pipeline looks at businesses marked won, and for each one whose paperwork actually came back — a client record with a signing date and no repo yet — it writes a short brief from what is already on the row (name, city, site, trade, contact, what we know, the notes from the call, the quote), asks the `/new-client-repo` skill to build the client repo from that brief, then asks `/intake` to write the request checklist and the request email. The client's ~/os index flips from `status: lead` to `status: in-progress` — only the line in the file's frontmatter, never the words "status: lead" appearing in the prose. All of it is pushed to ~/os under one `onboard: <slug>` commit, and only then is the repo address recorded and the business moved to onboarded, so a tick that pushed nothing leaves the row untouched for the next one. Three separate gates decide who is eligible, and each was verified by deliberately breaking it and confirming the tests go red: a won business with no client record is skipped and noted; a business marked won by hand with the `won <amount>` reply, which carries no signing date, is skipped and noted rather than onboarded; and a business that already has a repo is passed over silently with no skill call at all, so a second run costs nothing and creates nothing. Creating a GitHub repository cannot be undone, so a dry run deliberately takes a different path from the other jobs: it asks the skill only what it *would* have run, records that, and stops before the intake step and before any mark is written. A run where the repo skill comes back without a repo address is recorded as an error rather than a half-finished onboarding. The handover email is new and goes to exactly one person, the address in the new `ONBOARD_NOTIFY_TO` setting, never to the internal list — subject "Signed: <name> — your turn", carrying the repo address, the checklist and the request email. With that setting unset nothing is mailed at all and an error is recorded; both the fail-closed guard and the one-recipient rule were verified by breaking each and watching the tests go red, and the recipient is checked against a written-out address so widening the list cannot quietly pass. The notification job's four-way template choice became a plain per-stage lookup in the same pass, with all four existing stages proven to route exactly as before. It also fixed a live break found while wiring: every job that pushes to ~/os — pitch, quote and the signed-contract path — had no way to actually run git on a real run and would have failed on the first live push. Fixed once at the source, with a test that fails if it is ever dropped again. 327 tests pass, none failing. |
| 21. Alert triage into draft pull requests | done — a mail to `alerts@` is now read like a prospect reply and turned into a draft pull request on the repo it names, at most three a day, and the same alert seen twice makes no second PR. Built with tests only; no real alert has arrived yet, so it has not fired live. |
| 22. Per-mailbox SMTP transport | done — `jobs/run.mjs` used to build one global nodemailer transport from `SMTP_USER`/`SMTP_PASS`, and `touch.mjs` called it with no argument, so every send used whatever transport happened to exist regardless of which mailbox actually claimed the slot. `touch.mjs` now hands the claimed mailboxes row to `transport(claimed)`, and `run.mjs` resolves credentials per address from `SMTP_MAILBOX_<KEY>_USER`/`_PASS` (`KEY` = the address upper-cased, non-alphanumerics collapsed to `_`), caching one transport per address. A mailbox with no resolvable credentials is skipped with its own event before a claim is even attempted, and the "never fall back" guarantee was proven the caution-guardrail way: the per-address lookup was deliberately broken to fall back to another mailbox's pair, the tests went red, then it was reverted. A documented fallback (`SMTP_USER`/`SMTP_PASS` when the mailbox's address equals `SMTP_MAILBOX_DEFAULT` or `MAIL_FROM`) keeps tonight's live single-mailbox pipeline sending with zero new secrets — proven the same way, by reinserting a literal `@bcn-services.com` default and watching the literal-scan test fail. A third mutation — leaking the transport's own `auth.pass` into a `sent` event's detail — was likewise proven caught by the leak test, then reverted. `poll`/`notify`'s existing no-argument call for internal mail is untouched: a missing mailbox argument still resolves straight to `SMTP_USER`/`SMTP_PASS`, cached under its own slot, byte-for-byte the old single-transport behaviour. Seeds a placeholder second mailbox row (`0024_second_outreach_mailbox.sql`, address `outreach@bcns-mail.example`, `status = 'paused'` — the real schema has no boolean `active` flag) that `activeMailboxes()` never selects until a human renames and activates it. `docs/NOTIFICATIONS.md`'s stale claim that outreach and internal mail have separate reputations is corrected to match the decided one-seat/two-alias/shared-reputation architecture, and now documents the env var scheme. 345 tests pass (11 new), lint clean. |
| 23. Poll reads every outreach mailbox | done — replies are collected from every outreach mailbox the runner has credentials for, not just the first, and a reply to a retired address is still routed to its business. Live use waits on the second domain; today one mailbox is active and `outreach@trybcns.com` sits paused with its credentials already on the runner. |

## Round 1 — command center (shipped 2026-08-25)

- Status: all 13 items done. Item 7's send half is now built (SMTP, 2026-08-25); items 7, 8 and 11 each have a manual setup step left. See REVIEW.md.
- Next: read REVIEW.md and click through its numbered list — ten of twelve surfaces are proven only to compile, not to paint.
- Blockers: none in code. Item 7 needs the bot mailbox `bot@bcn-services.com` created and five values put in `.env.local` before mail leaves the queue. See docs/NOTIFICATIONS.md.
- Last updated: 2026-08-25

| Item | Status |
|------|--------|
| 1. Migration harness + production ledger backfill | done — every migration on disk now replays end to end on a throwaway database, and the production database finally records which migrations have been applied, so the next deploy will not try to run them all again |
| 2. Migration 0009 — automation schema | done — the database now knows the difference between work a person did and work an agent did, keeps every employee's inbox private even from an admin, and can record which leads are being contacted automatically and which are being handled by hand. A member can no longer forge an agent's audit trail. |
| 3. Agent verb layer | done — an agent can now look up and change leads, clients, tasks and activity through a fixed set of typed commands instead of a shell, and each command checks who is asking before it touches anything. Money figures are removed automatically for anyone who is not an admin. Two serious holes were found and closed: a crafted web address could have made the site-reader fetch private internal servers. |
| 4. Activity capture and parsing | done — a person can now type what happened in plain English ("called Mike at Coventry Tuesday, wants a quote by Friday") and the system turns it into a proper logged record, showing it for correction before anything is saved. Relative dates resolve against the person's own local date, not UTC. Closing a task now asks its assignee to log what happened. The review also found the activity history was not trustworthy: an admin could quietly edit or delete any past record, agent-written ones included, and most records had no author at all. It is now append-only and stamps the author itself, so history cannot be rewritten. Two lead actions that had been failing at runtime were fixed. |
| 5. Skill buttons and job-function gating | done — employees can now run a skill straight from the page where that work happens: pitch and quote on a lead or client, intake on a client, lead generation and system-improvement for admins only. Which buttons a person sees follows their job: sales and admins see the client-facing ones, developers see none, because every developer skill needs a cloned repo. Hiding a button is only tidiness — the server still refuses a run the person is not allowed to make, whatever the browser claims. Every run is recorded with who started it and how it ended. |
| 6. Inbox | done — every employee now has a private inbox in the app, newest first, with unread marks and a count in the sidebar. Items link to whatever they are about, and replying to one logs the contact through the same review-before-saving step as the capture box. Privacy is enforced by the database itself, not by the page: nobody, admin included, can read or alter anyone else's inbox. The count in the sidebar is cached, so it does not query on every page you open. |
| 7. Notification routing | done, except sending — the system now decides on its own whether something belongs in your inbox, your email, or both. Email goes out for exactly three things: a scheduled run that failed, a task assigned to you, and a lead saying they want to meet. Briefings, successful runs and agent proposals stay in the inbox only. Everything reaches the inbox no matter what. Sending is now built, and it goes through a bcns mailbox rather than Resend: every notice the system sends goes to an employee, never to a lead, so no third-party provider is needed. Until the mailbox is created, emails are written out in full and parked in a queue rather than lost. docs/NOTIFICATIONS.md has the steps. |
| 8. Daily briefing | done — signing in kicks off a briefing in the background: the page appears at once with the card marked as building, and it fills itself in when ready, so nothing waits on a run that takes up to a minute. It covers everything since your last briefing rather than 'since yesterday', so a week away gives you one briefing for the week instead of seven stale ones. It runs at most once per 20 hours, proven safe against two sign-ins at the same moment, and a failed run leaves your window intact rather than silently eating it. The skill file itself is staged in the repo for you to install into ~/os. |
| 9. Job runner and first three jobs | done — scheduled work now has one place to live: a job opens a record of the run, does its work, closes it with a result, and sends the outcome through the notification rules from item 7. Three jobs ship with it. A daily sweep checks every client site that has an address and reports the ones that are down. A weekly check warns when an agent token is within 30 days of expiring, so nothing dies silently next August. A daily detector flags a client being onboarded who has gone quiet for a week — active and finished clients are ignored on purpose. Each job claims its own time window before running, proven safe against two schedulers firing at the same moment, so a doubled cron line cannot produce doubled work. Nothing runs on a timer inside the app; an outside scheduler calls a script, so it works with no laptop on. The cron lines are written down in docs/JOBS.md for when the droplet exists. |
| 10. Lead outreach lanes and sweep | done — leads now sit in one of four lanes and the system knows which. Every new lead starts on the robot's lane. The moment a person logs any contact with that lead, the robot stops touching it — enforced by the database itself, so it holds however the record was written. Three robot touches with no reply parks the lead as no-response and it gets no fourth. You can move any lead between lanes by hand from the leads page. The draft writer reads each lead's own website and writes a real business description from what the site actually says, not from the search phrase that found them — proven by a test that fails if the page text never reaches the model. Drafts are database rows; there is no way to send from here and no address to send to. The territory sweep only ever draws from the target list you set, and with an empty list it returns nothing rather than inventing a town. The review found the draft batch could never move past its first 25 leads and would have failed silently; that is fixed, and the job now stops itself before its own timeout instead of writing rows after it was declared failed. Honest limit: nothing yet records a robot touch, so in practice every lead stays on touch one until a sender exists. |
| 11. README export generator | done — each client's README in ~/os now fills in its own facts from the database. The generator writes only inside marked-off delimiters in the frontmatter and copies the hand-written prose body across untouched, byte for byte — tested against a body containing the delimiter itself as prose, Windows line endings, a code fence and unicode. It is strictly one-way: the database is the truth, the file is an export, and the generator never reads the file as input. Running it twice with no data change produces no diff at all, because nothing in the output carries a clock. No money figure ever reaches the file — a client with no monthly rate exports with the field simply absent, never a zero and never a guess. It runs nightly as a job and commits as bcns-os-bot, naming the real person in the commit body when a change traces to one. Not tested against the real ~/os on purpose: this run was not allowed to write there, so commits were exercised against a throwaway repository. |
| 12. Admin configuration surface | done — the admin page now carries four working panels: add or retire a trade-and-town lead target, set each employee's job function, read the history of every scheduled run with its log, and see each person's Claude seat with its expiry date. Retiring a lead target is a flag flip, never a delete — the leads already found through it stay exactly as they are, proven against a real database. No token value ever reaches the page: the test feeds the panel a record that actually carries the sealed token, renders the real HTML, and greps for those bytes. The re-enroll control is instructions only; it cannot log anyone in, by design, and a person's jobs stop until they re-enroll rather than quietly borrowing an admin's seat. A member asking for /admin gets a 403, not a redirect. It also fixed a flaw flagged in item 9: a nightly run that worked fine but found a site down was being recorded as failed. Those are now two different outcomes, so the history reads honestly — and the new one still emails you. |
| 13. Final integration pass and review handoff | done — the whole suite, the type checker, the linter and a production build all run clean together: 893 tests across 147 suites, none failing and none skipped, up from 217 at the start. A seed script fills every surface with obviously fake content — invented businesses, example.com addresses, 555 phone numbers — so nothing renders as an empty state, and it refuses to run against anything but a local database so it cannot reach production by accident. REVIEW.md is the handoff: what was built, what is genuinely verified against what merely compiles, every blocker, every deliberate omission, and a numbered list of what to click to see each feature working. |
